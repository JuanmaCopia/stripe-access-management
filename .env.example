# Phase 1 environment placeholder
# Shared example variables will live here as real integrations land.
#
# Local development convention:
# - apps/web uses apps/web/.env.local for Next.js runtime variables
# - apps/worker will use apps/worker/.env in later phases once runtime config exists

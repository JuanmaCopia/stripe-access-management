{
  "name": "@stripe-access-management/core",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --max-warnings=0",
    "test": "pnpm build && node --test dist/test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}

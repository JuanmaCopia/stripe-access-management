## 1. Metadata and SEO

- [x] 1.1 Add `Metadata` export to `apps/web/app/layout.tsx`.
- [x] 1.2 Add specific `Metadata` export to the public home page in `apps/web/app/page.tsx`.

## 2. Loading and Error States

- [x] 2.1 Create `apps/web/app/loading.tsx` with a basic skeleton or spinner.
- [x] 2.2 Create `apps/web/app/error.tsx` with a user-friendly error message and retry button.
- [x] 2.3 Create `apps/web/app/not-found.tsx` for 404 handling.

## 3. UI and Responsive Polish

- [x] 3.1 Update `apps/web/app/globals.css` to ensure mobile-friendly padding and typography.
- [x] 3.2 Add an empty state message to the dashboard if no articles exist.
